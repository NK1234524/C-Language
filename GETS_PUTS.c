//Interview concept

#include <stdio.h>

int main()
{
    char st[30];
    char s[23];
    
    
    gets(st); // The entered string is stored in st!
// Whereas the gets is used to to read multiple word in same line.
    //printf("%s", st);
    puts(st);   // In place of printf("%s", st); using puts so that new line will be formed.
       // In place of printf("%s", st); using puts so that new line will be formed.
    printf("hey\n");
    
    
    
    
    
    gets(s);
    puts(s);
    printf("How are you");

    return 0;
}
