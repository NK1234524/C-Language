#include <stdio.h>
int main(){

   // char str[]={'a','b','c'};
    char str[]="abc";
    for (int i = 0; i < 3; i++)
    {
        printf("The value is: %c\n", str[i]); //str[i] here denotes element of strings with i and replication of value in array.
    }
    


    return 0 ;
}