#include<stdio.h>
#include<string.h>
int main(){
    
    int a = strcmp("sidhu", "manav");  // Dpenda on the starting alphabet.if it starts with 1st one then it is 1 else -1 if equal then 0
    printf("%d",a );

    return 0 ;
}
